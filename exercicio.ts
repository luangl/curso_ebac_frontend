function multiplicar(a: number, b: number): number {
  return a * b;
}

function saudar(nome: string): string {
  return `Olá ${nome}`;
}

console.log(multiplicar(2, 3)); 
console.log(saudar("Luan")); 
